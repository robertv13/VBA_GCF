Option Explicit

Private Sub Worksheet_Activate()

    Dim timerStart As Double: timerStart = Timer: Call Start_Timer("wshFAC_Finale:Worksheet_Activate()")
    
    Application.ScreenUpdating = False
    
    'Set the zoom factor to 75% when this worksheet is activated
    ActiveWindow.Zoom = 75
    
    Me.Application.Calculation = xlCalculationAutomatic
    
    Application.ScreenUpdating = True
    
    Call End_Timer("wshFAC_Finale:Worksheet_Activate()", timerStart)
    
End Sub