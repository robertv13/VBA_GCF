Option Explicit

Private Sub Worksheet_Activate()

    Dim timerStart As Double: timerStart = Timer: Call Start_Timer("wshTEC_Local:Worksheet_Activate()")

    wshTEC_Local.EnableCalculation = True
    
    Call End_Timer("wshTEC_Local:Worksheet_Activate()", timerStart)

End Sub
